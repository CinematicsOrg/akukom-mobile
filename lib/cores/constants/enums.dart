enum SvgType {
  asset,
  network,
  file,
}

enum ImageTypes {
  network,
  file,
  asset,
  profile,
  none,
  svg,
  memory,
}

enum SnackbarType {
  error,
  success,
  info,
  normal,
}

enum SizeUnit {
  pixel,
  percent,
}

enum NavBarType {
  home,
  gallery,
  messages,
  profile,
}
